<Window x:Class="WPF_SlowkaProjekt.ChooseQuizPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_SlowkaProjekt"
        mc:Ignorable="d"
        Title="ChooseQuizPage" Height="450" Width="800">
    <Grid Background="WhiteSmoke">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Content="Wybierz w jakiej formie chcesz rozwiązywac quizy"/>
            <Button x:Name="ChooseUnit"  
                    Content="Wybierz dział" 
                    Click="ChooseUnit_Click"/>
        <Button x:Name="RandomUnit"
                    Content="Generuj działy losowo"
                    Click="RandomUnit_Click"/>
    </Grid>
</Window>
